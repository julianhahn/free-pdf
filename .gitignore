target/
.DS_Store

# Test photos stay local. The one in here is a scan of a private letter, and
# personal data in git history cannot be taken back. The tests generate the
# images they need themselves.
test_images/
