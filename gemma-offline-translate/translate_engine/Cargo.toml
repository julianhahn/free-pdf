[package]
name = "translate_engine"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

# No dependencies on purpose. Everything here is text handling plus one child
# process, and a crate that talks to no network cannot grow one by accident.
[dependencies]
