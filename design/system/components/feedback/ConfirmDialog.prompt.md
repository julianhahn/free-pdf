# ConfirmDialog

Every delete asks. Three cases: scan, page, photos — copy verbatim from readme.md.

```jsx
<ConfirmDialog title="Delete the 40 photos?"
  body="The PDF stays. Without the photos the pages can no longer be adjusted."
  confirmLabel="Delete photos" onConfirm={del} onCancel={close} />
```
