# EmptyState

The Scans screen with no folders yet.

```jsx
<EmptyState title="No scans yet"
  body="Tap New scan and photograph the pages, one after another. You can stop whenever you like."
  action={<Button>New scan</Button>} />
```
