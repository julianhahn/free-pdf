import * as React from "react";
export interface ErrorLineProps { children?: React.ReactNode; style?: React.CSSProperties }
/** The engine's own finished sentence, above the content. Never rewritten. */
export declare function ErrorLine(props: ErrorLineProps): JSX.Element;
