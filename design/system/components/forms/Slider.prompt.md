# Slider

An adjust control. It opens on the value the engine suggested and prints that value beside the label.

```jsx
<Slider label="Brightness" value={v} min={-50} max={50} suggested={12}
        minLabel="darker" maxLabel="brighter" onChange={setV} />
```
